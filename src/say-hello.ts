// name: String (parameternya akan string)
// :String (returnnya akan string juga)
export function sayHello(name: String): String {
  return `Hello ${name}`;
}

// jadi ini adalah function say-hello menggunakan typescript
// lalu selanjutnya bikin unit test nya menggunakan typescript juga
